#include <stdlib.h>
#include <string.h>
#include <stdio.h>

int main(int argc, char **argv, char **env)
{
	int i;

	// i = 0;
	// while (env[i] != 0)
	// {
	// 	printf("env : %s\n", env[i]);
	// 	//printf("getenv : %s\n\n", getenv(env[i]));
	// 	i++;
	// }
	return (257);
}